{
  "name": "gvdg-member-auth",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "GVDG member portal auth API (Cloudflare Worker): PDGA#/UDisc + PIN login, forced first-login PIN change, passkeys.",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "jose": "^5.9.6"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^2.1.8"
  }
}
